import { combineReducers } from 'redux'

const Reducer = combineReducers({
  // TODO: Add reducers
  user: () => ({
    user: {
      name: 'Nathan Walters',
      netid: 'nwalter2',
    },
  }),
})

export default Reducer
